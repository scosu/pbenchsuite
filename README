== PTestSuite ==

PTestSuite are multiple python scripts which will help you in automatically
executing multiple benchmarks and record the results. Also there is a monitor
interface to record the state of the system.
